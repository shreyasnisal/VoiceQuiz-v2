import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    container: {
        width: '100%',
    },
    btnStyle: {
        // backgroundColor: '#0A75CC',
        backgroundColor: '#0D92FF',
        borderRadius: 5,
        alignItems: 'center',
        justifyContent: 'center',
        flexDirection: "row",
    },
    title: {
        color: '#fff',
    }
})